apiVersion: 1

datasources:
  - name: IoT-SQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /var/lib/grafana/data/iot_project.db
    version: 1
    editable: false
